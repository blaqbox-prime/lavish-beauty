import { ClientRecord } from '@/types'
import React from 'react'

type ClientsCardList = {
    clientsList: ClientRecord[]
}

function ClientsCardList({clientsList}: ClientsCardList) {
  return (
    <div></div>
  )
}

export default ClientsCardList